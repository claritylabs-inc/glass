min-release-age=7

# npm 11 does not support a per-package release-age allowlist.
# Production deploy installs explicitly override this for our own
# @claritylabs/cl-sdk releases in .github/workflows/deploy-convex.yml.
